{
  "name": "find-optimal-time-slot",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --platform=node --target=node24 --outfile=dist/index.js",
    "test": "jest"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.10.1",
    "esbuild": "^0.27.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}
